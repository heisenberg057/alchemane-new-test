# Local/VPS override: HTTP bootstrap nginx until TLS certs are available.
# Remove this file (or replace volumes) after switching to full nginx.conf + certs.
services:
  nginx:
    volumes:
      - ./nginx.bootstrap.conf:/etc/nginx/conf.d/default.conf:ro
      - /var/www/certbot:/var/www/certbot:ro
